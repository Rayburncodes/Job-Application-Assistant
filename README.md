# Job-Application-Assistant

Next.js app for resume analysis and tailored job-application answers.

## Getting started

```bash
npm install
cp .env.example .env   # then fill in values
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser (use the port shown in the terminal if it differs).
